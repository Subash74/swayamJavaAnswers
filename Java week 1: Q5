result=arr[0];
	int total=arr[0];
	for(i=1;i<arr.length;i++)
	  {
        	if(arr[i]>result)
              result=arr[i];
      		total+=arr[i];
	  }
	mark_avg=total/arr.length;
	System.out.print(result +"\n" + mark_avg);